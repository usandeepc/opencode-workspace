{
  "id": "opencode",
  "version": "1.0.4",
  "name": "opencode",
  "description": "Installs the opencode CLI and provisions authentication/session sync with a persistent volume.",
  "documentationURL": "https://github.com/usandeepc/opencode-workspace",
  "licenseURL": "https://github.com/usandeepc/opencode-workspace",
  "keywords": ["opencode", "ai", "assistant"],
  "installsAfter": ["ghcr.io/devcontainers/features/common-utils:2"],
  "options": {
    "version": {
      "type": "string",
      "description": "opencode CLI version to install (pinned; avoid floating latest)",
      "proposals": ["1.18.29"],
      "default": "1.18.29"
    }
  },
  "containerEnv": {
    "PATH": "${PATH}:${HOME}/.opencode/bin"
  },
  "mounts": [
    {
      "source": "opencode-data",
      "target": "${HOME}/.local/share/opencode",
      "type": "volume"
    }
  ],
  "onCreateCommand": "mkdir -p ${HOME}/.local/share/opencode ${HOME}/.opencode-host-sync && if [ -f ${HOME}/.opencode-host-sync/auth.json ]; then cp ${HOME}/.opencode-host-sync/auth.json ${HOME}/.local/share/opencode/auth.json && chmod 600 ${HOME}/.local/share/opencode/auth.json; fi"
}
